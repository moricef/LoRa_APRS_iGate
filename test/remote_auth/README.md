# Remote command authentication vectors

Run the implementation-independent HMAC vectors and the production replay
window tests with:

```sh
make -C test/remote_auth test
```

The published vector is also recorded in both remote-control design documents.
